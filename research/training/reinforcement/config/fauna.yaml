name: "FaunaModel"
description: "Modelo de Reinforcement para el agente de fauna"
version: "0.0.1"
model:
  algorithm: "DQN"
  policy: "MultiInputPolicy"
  hyperparams:
    learning_rate: 5.0e-4
    buffer_size: 500000
    learning_starts: 10000
    batch_size: 64
    gamma: 0.99
    tau: 0.005             # Factor de actualización para la red target
    train_freq: 4          # Frecuencia de updates, steps básicamente
    target_update_interval: 5000
    exploration_fraction: 0.6
    exploration_initial_eps: 1.0
    exploration_final_eps: 0.2
    max_grad_norm: 1.0
    verbose: 1
    tensorboard_log: "./tensorboard_log/dqn/"
  timesteps: 200000
environment:
  env_class: "FaunaEnvironment"
  observation_space:
    type: "Dict"
    spaces:
      energy: [ 0, 100 ]
      health: [ 0, 100 ]
      nearby_food: [ 0, 10 ]
      nearby_predators: [ 0, 5 ]
      temperature: [ 0, 50 ]
  action_space:
    type: "Discrete"
    n: 6
output_path: "./models/fauna_model_final"
